bin/
obj/
node_modules/
dist/
.vite/
data/
.fake/
.ionide/
*.user
.DS_Store
.env

# Auto Claude data directory
.auto-claude/

# Agentheim machine-written runtime/advisory artifacts (ADR-0027) — local only,
# never committed. Tracked agentheim content (vision, contexts, knowledge) is NOT
# ignored; only these generated dirs are.
.agentheim/.dashboard/
.agentheim/state/
